# Spotify Streaming Analytics Platform

An end-to-end Data Engineering project that ingests Spotify metadata, stores raw JSON, processes data through a Medallion Architecture, loads curated datasets into Snowflake, and visualizes insights using Power BI.